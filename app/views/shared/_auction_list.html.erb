<% paginate_auctions %>
<p>
<% @page.auctions['auctions'].each do |auction| %>
  <div class="auction_thumb_list">
    <%= link_to image_tag(auction.image_icon), auction_path(auction) %> 
    <%= auction.title %>
    <%= yen auction.current_price %> <br>
  </div>
<% end %>